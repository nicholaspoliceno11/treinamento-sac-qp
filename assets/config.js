/*
 * Configuração do portal de treinamento.
 *
 * API_URL: cole aqui a URL do seu Google Apps Script publicado como "App da Web".
 * Passo a passo em: apps-script/README.md
 *
 * Enquanto estiver vazio, o site funciona em "modo demonstração" (sem login real).
 * Para desenvolvimento local também é possível sobrescrever a URL via console:
 *   localStorage.setItem('qp_api_url', 'http://localhost:8787')
 */
window.QP_CONFIG = {
  API_URL: ""
};
